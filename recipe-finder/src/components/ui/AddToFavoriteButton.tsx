import { Heart } from "lucide-react"

export default function AddToFavoriteButton() {
  return (
    <div className="add-to-favorites-btn text-(--clr-white)">
      <Heart size={35} />
    </div>
  )
}